<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.chigua.mapper.AdvertisementMapper">
    
    <resultMap type="Advertisement" id="AdvertisementResult">
        <id     property="id"                   column="id"                   />
        <result property="title"               column="title"               />
        <result property="description"         column="description"         />
        <result property="adType"              column="ad_type"             />
        <result property="appType"             column="app_type"            />
        <result property="position"            column="position"            />
        <result property="categoryId"          column="category_id"         />
        <result property="imageUrl"            column="image_url"           />
        <result property="iconName"            column="icon_name"           />
        <result property="linkUrl"             column="link_url"            />
        <result property="linkText"            column="link_text"           />
        <result property="clickCount"          column="click_count"         />
        <result property="impressionCount"     column="impression_count"    />
        <result property="sortOrder"           column="sort_order"          />
        <result property="startDate"           column="start_date"          />
        <result property="endDate"             column="end_date"            />
        <result property="status"              column="status"              />
        <result property="createdAt"           column="created_at"          />
        <result property="updatedAt"           column="updated_at"          />
        <result property="isGlobal"            column="is_global"           />
    </resultMap>

    <sql id="selectAdvertisementVo">
        select id, title, description, ad_type, app_type, position, category_id, image_url, icon_name, link_url, link_text, 
               click_count, impression_count, sort_order, start_date, end_date, status, created_at, updated_at, is_global 
        from advertisements
    </sql>

    <select id="selectAdvertisementList" parameterType="Advertisement" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        <where>  
            <if test="title != null and title != ''"> and title like concat('%', #{title}, '%')</if>
            <if test="adType != null and adType != ''"> and ad_type = #{adType}</if>
            <if test="appType != null and appType != ''"> and app_type = #{appType}</if>
            <if test="position != null and position != ''"> and position = #{position}</if>
            <if test="status != null"> and status = #{status}</if>
            <if test="isGlobal != null"> and is_global = #{isGlobal}</if>
        </where>
        order by sort_order desc, id desc
    </select>
    
    <select id="selectAdvertisementById" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where id = #{id}
    </select>

    <select id="checkAdvertisementTitleUnique" parameterType="String" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where title = #{title}
    </select>

    <select id="selectAdvertisementByPosition" parameterType="String" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 and position = #{position}
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id desc
    </select>

    <select id="selectAdvertisementByCategory" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and (is_global = 1 or find_in_set(#{categoryId}, category_id))
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id desc
    </select>
        
    <insert id="insertAdvertisement" parameterType="Advertisement" useGeneratedKeys="true" keyProperty="id">
        insert into advertisements
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">title,</if>
            <if test="description != null">description,</if>
            <if test="adType != null and adType != ''">ad_type,</if>
            <if test="appType != null">app_type,</if>
            <if test="position != null">position,</if>
            <if test="categoryId != null">category_id,</if>
            <if test="imageUrl != null">image_url,</if>
            <if test="iconName != null">icon_name,</if>
            <if test="linkUrl != null">link_url,</if>
            <if test="linkText != null">link_text,</if>
            <if test="clickCount != null">click_count,</if>
            <if test="impressionCount != null">impression_count,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="startDate != null">start_date,</if>
            <if test="endDate != null">end_date,</if>
            <if test="status != null">status,</if>
            <if test="isGlobal != null">is_global,</if>
         </trim>
         <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">#{title},</if>
            <if test="description != null">#{description},</if>
            <if test="adType != null and adType != ''">#{adType},</if>
            <if test="appType != null">#{appType},</if>
            <if test="position != null">#{position},</if>
            <if test="categoryId != null">#{categoryId},</if>
            <if test="imageUrl != null">#{imageUrl},</if>
            <if test="iconName != null">#{iconName},</if>
            <if test="linkUrl != null">#{linkUrl},</if>
            <if test="linkText != null">#{linkText},</if>
            <if test="clickCount != null">#{clickCount},</if>
            <if test="impressionCount != null">#{impressionCount},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="startDate != null">#{startDate},</if>
            <if test="endDate != null">#{endDate},</if>
            <if test="status != null">#{status},</if>
            <if test="isGlobal != null">#{isGlobal},</if>
         </trim>
    </insert>

    <update id="updateAdvertisement" parameterType="Advertisement">
        update advertisements
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="adType != null and adType != ''">ad_type = #{adType},</if>
            <if test="appType != null">app_type = #{appType},</if>
            <if test="position != null">position = #{position},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="imageUrl != null">image_url = #{imageUrl},</if>
            <if test="iconName != null">icon_name = #{iconName},</if>
            <if test="linkUrl != null">link_url = #{linkUrl},</if>
            <if test="linkText != null">link_text = #{linkText},</if>
            <if test="clickCount != null">click_count = #{clickCount},</if>
            <if test="impressionCount != null">impression_count = #{impressionCount},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="status != null">status = #{status},</if>
            <if test="isGlobal != null">is_global = #{isGlobal},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteAdvertisementById" parameterType="Long">
        delete from advertisements where id = #{id}
    </delete>

    <delete id="deleteAdvertisementByIds" parameterType="String">
        delete from advertisements where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateClickCount" parameterType="Long">
        update advertisements set click_count = click_count + 1 where id = #{id}
    </update>

    <update id="updateImpressionCount" parameterType="Long">
        update advertisements set impression_count = impression_count + 1 where id = #{id}
    </update>

    <!-- Web广告查询 - 根据位置 -->
    <select id="selectAdsByPosition" parameterType="String" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and position = #{position}
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <!-- Web广告查询 - 根据类型 -->
    <select id="selectAdsByType" parameterType="String" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and ad_type = #{adType}
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <select id="selectWebCategoryAds" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and position = '1'
        and find_in_set(#{categoryId}, category_id)
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <select id="selectWebCategoryBottomAds" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and position = '2'
        and find_in_set(#{categoryId}, category_id)
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <select id="selectLogoAdsByAppType" parameterType="String" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and ad_type = '2'
        and app_type = #{appType}
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <!-- 根据多个位置和分类查询广告 -->
    <select id="selectAdsByPositionsAndCategory" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and position in 
        <foreach item="position" collection="positions" open="(" separator="," close=")">
            #{position}
        </foreach>
        and find_in_set(#{categoryId}, category_id)
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <!-- 定时任务相关查询 -->
    
    <!-- 查询即将到期的广告（指定天数内） -->
    <select id="selectExpiringAdvertisements" parameterType="int" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        WHERE status = 1 
        AND end_date IS NOT NULL 
        AND end_date BETWEEN CURDATE() AND DATE_ADD(CURDATE(), INTERVAL #{days} DAY)
        ORDER BY end_date ASC
    </select>

    <!-- 查询已过期但仍启用的广告 -->
    <select id="selectExpiredAdvertisements" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        WHERE status = 1 
        AND end_date IS NOT NULL 
        AND end_date &lt; CURDATE()
        ORDER BY end_date DESC
    </select>

    <!-- 批量禁用过期广告 -->
    <update id="disableExpiredAdvertisements">
        UPDATE advertisements 
        SET status = 0, updated_at = NOW()
        WHERE status = 1 
        AND end_date IS NOT NULL 
        AND end_date &lt; CURDATE()
    </update>

    <!-- 根据分类ID获取短视频广告 -->
    <select id="selectShortVideoAdsByCategory" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and ad_type = '5'
        and (is_global = 1 or find_in_set(#{categoryId}, category_id))
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

    <!-- 根据分类ID获取分页模式广告 -->
    <select id="selectPagedAdsByCategory" parameterType="Long" resultMap="AdvertisementResult">
        <include refid="selectAdvertisementVo"/>
        where status = 1 
        and ad_type = '6'
        and (is_global = 1 or find_in_set(#{categoryId}, category_id))
        and (start_date is null or start_date &lt;= curdate())
        and (end_date is null or end_date &gt;= curdate())
        order by sort_order desc, id asc
    </select>

</mapper> 